import React from 'react';

const ClientReview = ({review}) => {

    const {date, body, title, image_url} = review;

    return (
        <div>
            
        </div>
    );
};

export default ClientReview;